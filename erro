for item in filtered_texts:
                        if "text" in item:
                            text = item["text"]
                            if text not in unique_texts:
                                texts_to_join.append(text)
                                unique_texts.add(text)

                    top_doc_context_1 = "\n --- Document --- \n".join(texts_to_join)
